# دليل إعداد Decap CMS لموقع ALPHA

## ✅ الملفات التي تم إنشاؤها

تم إنشاء الملفات التالية بنجاح:

1. **`admin/index.html`** - صفحة لوحة التحكم
2. **`admin/config.yml`** - ملف إعدادات Decap CMS
3. **`blog/`** - مجلد لحفظ المقالات
4. تم تحديث **`index.html`** بإضافة Netlify Identity Widget

---

## 📋 خطوات الإعداد على Netlify

### 1. رفع الموقع إلى GitHub
```bash
git init
git add .
git commit -m "إضافة Decap CMS"
git branch -M main
git remote add origin <رابط-المستودع-الخاص-بك>
git push -u origin main
```

### 2. ربط الموقع مع Netlify
1. اذهب إلى [netlify.com](https://netlify.com)
2. سجل دخول أو أنشئ حساب جديد
3. اضغط على **"Add new site"** ← **"Import an existing project"**
4. اختر **GitHub** وحدد مستودعك
5. اضغط **Deploy site**

### 3. تفعيل Git Gateway
1. في لوحة تحكم Netlify، اذهب إلى **Site settings** ← **Identity**
2. اضغط **Enable Identity**
3. في قسم **Registration preferences**، اختر **Invite only** (للأمان)
4. في قسم **Services**، اضغط **Enable Git Gateway**

### 4. إنشاء مستخدم
1. اذهب إلى تبويب **Identity** في لوحة التحكم
2. اضغط **Invite users**
3. أدخل بريدك الإلكتروني
4. ستصلك رسالة تأكيد، افتحها وأكمل التسجيل

---

## 🚀 كيفية الوصول إلى لوحة التحكم

بعد نشر الموقع على Netlify:

1. اذهب إلى: `https://اسم-موقعك.netlify.app/admin/`
2. سجل دخول باستخدام بريدك الإلكتروني
3. ستظهر لك لوحة التحكم لإدارة المقالات

---

## 📝 كيفية إضافة مقال جديد

1. افتح لوحة التحكم `/admin/`
2. اضغط على **"المقالات"**
3. اضغط **"New المقالات"**
4. املأ الحقول:
   - **العنوان**: عنوان المقال
   - **وصف قصير**: ملخص المقال
   - **تاريخ النشر**: اختر التاريخ والوقت
   - **صورة المقال**: ارفع صورة
   - **محتوى المقال**: اكتب المحتوى بصيغة Markdown
5. اضغط **Publish** ← **Publish now**

---

## 🔧 تخصيص الإعدادات

يمكنك تعديل ملف `admin/config.yml` لإضافة المزيد من المجموعات أو الحقول.

### مثال: إضافة صفحة "من نحن"
```yaml
collections:
  - name: "pages"
    label: "الصفحات"
    files:
      - label: "من نحن"
        name: "about"
        file: "content/about.md"
        fields:
          - { label: "العنوان", name: "title", widget: "string" }
          - { label: "المحتوى", name: "body", widget: "markdown" }
```

---

## 📂 بنية المشروع

```
alpha-portfolio/
├── admin/
│   ├── index.html          # صفحة لوحة التحكم
│   └── config.yml          # إعدادات CMS
├── blog/                   # مجلد المقالات
├── assets/
│   └── img/                # الصور المرفوعة
├── css/
├── js/
└── index.html              # الصفحة الرئيسية (مع Netlify Identity)
```

---

## ⚠️ ملاحظات مهمة

1. **الأمان**: تأكد من تفعيل **Invite only** في إعدادات Identity
2. **النسخ الاحتياطي**: المقالات تُحفظ في مستودع GitHub تلقائياً
3. **الصور**: تُحفظ في مجلد `assets/img/`
4. **اللغة**: الواجهة مُعدة للعربية بالكامل

---

## 🆘 حل المشاكل الشائعة

### المشكلة: لا يمكن الوصول إلى `/admin/`
- **الحل**: تأكد من رفع مجلد `admin/` إلى GitHub ونشره على Netlify

### المشكلة: خطأ في تسجيل الدخول
- **الحل**: تأكد من تفعيل Git Gateway في إعدادات Netlify

### المشكلة: لا تظهر المقالات
- **الحل**: تأكد من وجود مجلد `blog/` في المستودع

---

## 📞 دعم إضافي

- [توثيق Decap CMS](https://decapcms.org/docs/)
- [توثيق Netlify Identity](https://docs.netlify.com/visitor-access/identity/)

---

**تم الإعداد بنجاح! 🎉**
